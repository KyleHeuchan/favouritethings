const config = {
    user: "root",
    pword: "", // windows users leave this blank mac only needs this
    host: "localhost",
    port: "3306", // windows users - sql port is 3306
    database: "videogames"
}

module.exports = config;